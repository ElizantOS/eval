[project]
name = "minimal-eval-workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["eval"]

[tool.uv.sources]
eval = { git = "ssh://git@github.com/ElizantOS/eval.git", rev = "main" }
